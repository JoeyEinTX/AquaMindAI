# AquaMind Environment Configuration

# =============================================================================
# GEMINI API KEY (Required for AI features)
# =============================================================================
GEMINI_API_KEY=your_gemini_api_key_here

# Default ZIP code for weather and advisory features (optional)
DEFAULT_ZIP_CODE=78701

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================
# Base URL for the AquaMind backend API
VITE_API_BASE_URL=http://localhost:3001

# WebSocket URL for realtime frontend updates
VITE_WS_BASE_URL=http://localhost:3003

# WebSocket port for backend server
WS_PORT=3003

# =============================================================================
# RELAY DRIVER CONFIGURATION
# =============================================================================

# Relay Mode: mock | gpio | http
# - mock: Simulates relay operations (default, safe for development)
# - gpio: Controls Raspberry Pi GPIO pins
# - http: Sends HTTP requests to external relay controller (ESP32, etc.)
RELAY_MODE=mock

# -----------------------------------------------------------------------------
# GPIO Mode Configuration (only needed if RELAY_MODE=gpio)
# -----------------------------------------------------------------------------
# GPIO pin mapping format: zoneId:gpioPin,zoneId:gpioPin,...
# Default mapping if not specified: 1:17,2:18,3:27,4:22
# GPIO_PIN_MAP=1:17,2:18,3:27,4:22

# -----------------------------------------------------------------------------
# HTTP Mode Configuration (only needed if RELAY_MODE=http)
# -----------------------------------------------------------------------------
# Base URL of your relay controller (ESP32, Arduino, etc.)
# RELAY_BASE_URL=http://192.168.1.100:8080

# =============================================================================
# AI MEMORY & LEARNING CONFIGURATION
# =============================================================================
# Enable adaptive memory and learning (true by default)
# Set to false to disable persistent storage of user interactions
LEARNING_ENABLED=true

# =============================================================================
# ENVIRONMENT NOTES
# =============================================================================

# DEVELOPMENT:
# - Use default localhost URLs for local development
# - RELAY_MODE=mock is safe for testing without hardware

# PRODUCTION (Raspberry Pi):
# - Set VITE_API_BASE_URL and VITE_WS_BASE_URL to Pi's IP (e.g., http://192.168.1.100:3001)
# - Set RELAY_MODE=gpio
# - Configure GPIO_PIN_MAP for your relay wiring

# PRODUCTION (Cloud/Remote):
# - Set VITE_API_BASE_URL and VITE_WS_BASE_URL to your domain
# - Set RELAY_MODE=http
# - Configure RELAY_BASE_URL to point to relay controller

# =============================================================================
# EXAMPLES
# =============================================================================

# Example 1: Desktop Development (Mock Mode)
# VITE_API_BASE_URL=http://localhost:3001
# VITE_WS_BASE_URL=http://localhost:3003
# RELAY_MODE=mock

# Example 2: Raspberry Pi Production (GPIO Mode)
# VITE_API_BASE_URL=http://192.168.1.100:3001
# VITE_WS_BASE_URL=http://192.168.1.100:3003
# RELAY_MODE=gpio
# GPIO_PIN_MAP=1:17,2:18,3:27,4:22

# Example 3: Cloud Deployment (HTTP Mode)
# VITE_API_BASE_URL=https://aquamind.yourdomain.com
# VITE_WS_BASE_URL=https://aquamind.yourdomain.com
# RELAY_MODE=http
# RELAY_BASE_URL=http://192.168.1.100:8080
