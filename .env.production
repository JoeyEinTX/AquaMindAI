# AquaMind Production Environment Configuration

# =============================================================================
# GEMINI API KEY (Required for AI features)
# =============================================================================
GEMINI_API_KEY=your_production_gemini_api_key_here

# =============================================================================
# BACKEND API CONFIGURATION
# =============================================================================
# Base URL for the AquaMind backend API (use your domain or Pi IP)
VITE_API_BASE_URL=http://localhost:3001

# WebSocket URL for realtime frontend updates (use your domain or Pi IP)
VITE_WS_BASE_URL=http://localhost:3003

# WebSocket port for backend server
WS_PORT=3003

# Backend API port
PORT=3001

# =============================================================================
# RELAY DRIVER CONFIGURATION
# =============================================================================

# Relay Mode: mock | gpio | http
# - mock: Simulates relay operations (safe for testing production build)
# - gpio: Controls Raspberry Pi GPIO pins (production on Pi)
# - http: Sends HTTP requests to external relay controller
RELAY_MODE=gpio

# -----------------------------------------------------------------------------
# GPIO Mode Configuration (for Raspberry Pi deployment)
# -----------------------------------------------------------------------------
# GPIO pin mapping format: zoneId:gpioPin,zoneId:gpioPin,...
GPIO_PIN_MAP=1:17,2:18,3:27,4:22

# -----------------------------------------------------------------------------
# HTTP Mode Configuration (for external relay controller)
# -----------------------------------------------------------------------------
# Base URL of your relay controller (ESP32, Arduino, etc.)
# RELAY_BASE_URL=http://192.168.1.100:8080

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# 1. For Raspberry Pi: Set RELAY_MODE=gpio and configure GPIO_PIN_MAP
# 2. For cloud/remote: Use RELAY_MODE=http and set RELAY_BASE_URL
# 3. Update VITE_API_BASE_URL and VITE_WS_BASE_URL with your public IP/domain
# 4. Example for Pi on local network: http://192.168.1.100:3001
# 5. Example for cloud: https://aquamind.yourdomain.com
